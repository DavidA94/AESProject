@model AES.Web.Models.FullApplicationModel

@{
    ViewBag.Title = "CallPage";
}

<h2>@Model.Profile.Address</h2>



@*<div class="row">
    <div class="col-md-12">
        <section id="CallPage">
            @using (Html.BeginForm("UserProfile", "Application", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
                <h4>Tell us about yourself.</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                // Name & DOB
                <div class="form-group">
                    <div class="col-md-8 staticText">
                        <p><span>Name:</span> @Model.Profile.Nickname </p>
                        <p><span>City:</span> @Model.Profile.City</p>

                    </div>
                </div>
            }

        </section>
    </div>
</div>*@